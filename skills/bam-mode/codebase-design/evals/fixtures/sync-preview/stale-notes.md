# Superseded architecture notes

- Rename the command to `session pull`.
- Convert the package to TypeScript under `src/services/`.
- Add a `SyncTransport` plugin and fake client for preview.
- Let CLI and normal execution implement their own ordering.
