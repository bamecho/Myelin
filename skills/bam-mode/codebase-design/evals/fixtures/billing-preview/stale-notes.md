# Superseded architecture notes

- Merge all modules into a `BillingEngine` class.
- Delete focused normalization and selection tests.
- Add `StripePort`, `StripeAdapter`, and `FakeStripeAdapter`.
- Convert the package to TypeScript.
